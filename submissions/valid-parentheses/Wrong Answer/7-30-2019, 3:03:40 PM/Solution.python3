// https://leetcode.com/problems/valid-parentheses

class Solution:
    def isValid(self, s: str) -> bool:
        if len(s)==0:
           return True
        if len(s)==1:
           return False

        stack = []
        stack.append(s[0])
        
        for i in range(1,len(s)):
            tmp_char = s[i]
            
            # stack is empty
            if len(stack)==0:
                stack.append(tmp_char)
            
            top = stack[-1]
            if ( top=="(" and tmp_char==")" ) or ( top=="{" and tmp_char=="}" ) or ( top=="[" and tmp_char=="]" ):
                stack.pop()
        
        if len(stack)==0:
            return True
        else:
            return False
        
            

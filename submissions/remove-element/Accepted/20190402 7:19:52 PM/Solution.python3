// https://leetcode.com/problems/remove-element

class Solution:
    def removeElement(self, nums, val):
        while True:
            try:
                i = nums.index(val)
                del nums[i]
            except ValueError:
                return len(nums)